# ERP Duo Print 3D — Correção 0.5.1

Correções:
- três construtores `pw.TextStyle` definidos como `const`;
- importação não utilizada removida de `billing_page.dart`;
- versão atualizada para 0.5.1.

## Instalação

Copie `lib` e `pubspec.yaml` sobre o projeto atual e execute:

```cmd
flutter clean
flutter pub get
flutter analyze
flutter run
```
